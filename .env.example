# ============================================
# Environment Configuration Example
# ============================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env
#
# This file contains sensitive credentials and API keys
# DO NOT commit .env to version control (it's in .gitignore)
# ============================================

# Database Configuration
# ============================================
# PostgreSQL connection string format:
# postgresql://username:password@host:port/database_name
DB_USER=cyber_admin
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432
DB_NAME=cybersecurity

# Full Database URI (constructed from above variables)
DATABASE_URI=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Flask Configuration
# ============================================
FLASK_APP=backend/app.py
FLASK_ENV=development
SECRET_KEY=your_secret_key_here_change_in_production

# Server Configuration
# ============================================
HOST=127.0.0.1
PORT=5000
DEBUG=True

# API Keys (for external services)
# ============================================
# Twitter API credentials (if using Twitter data collection)
TWITTER_API_KEY=your_twitter_api_key
TWITTER_API_SECRET=your_twitter_api_secret
TWITTER_ACCESS_TOKEN=your_twitter_access_token
TWITTER_ACCESS_SECRET=your_twitter_access_secret

# News API (if using news data collection)
NEWS_API_KEY=your_news_api_key

# Machine Learning Model
# ============================================
ML_MODEL_PATH=ml/models/threat_classifier.pkl

# Logging
# ============================================
LOG_LEVEL=INFO
LOG_FILE=backend/logs/server.log

# ============================================
# RELATIONSHIP WITH config/*.yaml FILES
# ============================================
# .env file: Stores SENSITIVE credentials (passwords, API keys)
# config/development.yaml: Non-sensitive development environment settings
# config/production.yaml: Non-sensitive production environment settings
#
# For basic local development, you only need to configure this .env file.
# The config/*.yaml files are used for advanced deployment scenarios where
# you need different configurations per environment (dev/staging/prod).
